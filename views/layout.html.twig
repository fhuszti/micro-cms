<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8" />
        <link href="{{ asset('/lib/bootstrap/css/bootstrap.min.css') }}" rel="stylesheet" />
        <link href="{{ asset('/css/style.css') }}" rel="stylesheet" />
        <title>{% block title %}{% endblock %} - MicroCMS</title>
    </head>
    <body>
        <header class="navbar navbar-default navbar-fixed-top">
            <div class="container">
                <div class="navbar-header">
                    <a href="{{ path('home') }}" title="Retour à la page d'accueil" class="navbar-brand">
                        MicroCMS
                    </a>

                    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </button>
                </div>
                <div class="collapse navbar-collapse">
                    <nav role="navigation">
                        <!-- actual menu list -->
                        <ul class="nav navbar-nav navbar-right">
                            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                                {% if app.user and is_granted('ROLE_ADMIN') %}
                                    <li class="{% if adminMenu is defined %}active{% endif %}">
                                        <a href="{{ path('admin') }}" title="Espace d'administration">
                                            <span class="glyphicon glyphicon-cog"></span> Administration
                                        </a>
                                    </li>
                                {% endif %}
                                <li class="dropdown">
                                    <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                                        <span class="glyphicon glyphicon-user"></span> Bienvenue, {{ app.user.username }} <b class="caret"></b>
                                    </a>
                                    <ul class="dropdown-menu">
                                        <li>
                                            <a href="{{ path('logout') }}" title="Déconnexion">Se déconnecter</a>
                                        </li>
                                    </ul>
                                </li>
                            {% else %}
                                <li class="dropdown">
                                    <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                                        <span class="glyphicon glyphicon-user"></span> Vous n'êtes pas connecté <b class="caret"></b>
                                    </a>
                                    <ul class="dropdown-menu">
                                        <li>
                                            <a href="{{ path('login') }}" title="Connexion">Se connecter</a>
                                        </li>
                                    </ul>
                                </li>
                            {% endif %}
                        </ul>
                    </nav>
                </div>
            </div>
        </header>

        <div id="wrapper" class="container" role="main">
            {% block content %}{% endblock %}
        </div>

        <footer class="footer">
            <a href="https://github.com/fhuszti/micro-cms">MicroCMS</a> is a minimalistic CMS built as a showcase for modern PHP development.
        </footer>

        <!-- jQuery -->
        <script src="{{ asset('/lib/jquery/jquery.min.js') }}"></script>

        <!-- JavaScript Boostrap plugin -->
        <script src="{{ asset('/lib/bootstrap/js/bootstrap.min.js') }}"></script>
    </body>
</html>
